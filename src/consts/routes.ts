import { RouteType } from '@/types';

const Routes: {
  dashboard: RouteType;
  login: RouteType;
} = {
  dashboard: {
    path: '/',
  },
  login: {
    path: '/login',
  },
};

export default Routes;
